# Sidecar target (optional). If unset, status panel shows "unconfigured".
SIDECAR_BASE_URL=http://127.0.0.1:8000

# Policy file path (default points to example; set to your active policy location)
POLICY_PATH=enterprise_sidecar/policy/policy_v1.example.json

# Receipts directory (point to your sidecar receipt output directory)
RECEIPTS_DIR=ui/runtime/receipts

# Read-only mode (recommended default). Set to 0 to enable Apply Policy.
READ_ONLY=1
